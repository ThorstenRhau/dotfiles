# ── OS ─────────────────────────────────────────────────────────────────────────
# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
desktop.ini
$RECYCLE.BIN/
*.lnk

# Linux
*~                                  # backup/temp files
*.swp                               # vim swap files

# ── Shell scripts ─────────────────────────────────────────────────────────────
*.tmp
*.bak
# session/history
command.log
bash_history

# ── Python ────────────────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*.pyo
*.pyd
*.egg-info/
build/
dist/
*.egg
env/
.env
.env.*
!.env.example
!.env.sample
!.env.template
venv/
.venv/

# ── Lua ───────────────────────────────────────────────────────────────────────
*.luac
luac.out

# ── General ───────────────────────────────────────────────────────────────────
# lock files
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# editor swap files
*.sw?
*.swo
*.swn

# Assorted dot files that are not to be handled in this repo
**/.claude/settings.local.json
